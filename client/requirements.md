## Packages
recharts | For portfolio allocation pie/donut charts
framer-motion | For page transitions and the stock card swiping animations
lucide-react | For all UI icons

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Plus Jakarta Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
Auth is handled via Replit integration hooks (useAuth, redirectToLogin).
